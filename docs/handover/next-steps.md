# Next Steps

1. Create Google OAuth client + Facebook Login app; paste IDs/secrets into `apps/api/.env`
2. Authorized redirect URIs must match `GOOGLE_CALLBACK_URL` / `FACEBOOK_CALLBACK_URL`
3. Restart API (and worker if stopped); smoke Continue with Google on `/login`
4. Later candidates: North-Indian kundali, WhatsApp approved templates
